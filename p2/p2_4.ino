/*void strTochar(String txt, char* c){
  if(WiFi.status() != WL_CONNECTED){
    int len = txt.length();
    Serial.println(len);
    Serial.println("*************");
    c = (char*)malloc(len);
    for(int i = 0; i<len; i++){
      c[i] = txt[i];
      Serial.print(c[i]);
      }
    }
  }*/
